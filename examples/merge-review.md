# Merge Review Example

Use `merge-readiness-gate` after implementation, verification, and review evidence exist:

```text
Use merge-readiness-gate for main..HEAD. Do not merge or push.
```

Expected output states READY, BLOCKED, or NEEDS HUMAN DECISION with evidence.
