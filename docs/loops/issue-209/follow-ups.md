# Issue #209 Follow-ups

- Owner: maintainer. Target: independent human decision after synthetic pilot
  evidence; reason: synthetic results do not establish real-user efficacy.
- Owner: future scoped proposal. Target: any shared-host, encryption, purge,
  migration, provider, automatic lifecycle, or V3-C work; reason: explicitly
  excluded from this thin local pilot.
- Owner: maintainer. Target: a future pilot revision if class-complete recall is
  required; reason: the unchanged M1 adapter applies its bounded result limit
  before the façade selects one pilot class, so other classes can produce a
  fail-closed false negative. Verification: add a class-crowding fixture and a
  storage-layer class index. Promotion trigger: class-complete recall becomes a
  product requirement rather than best-effort advisory context.
